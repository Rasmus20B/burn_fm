test BasicTest:
  script: [
    define blank_test() {
      set_variable(x, 1);
      loop {
        new_record_request();
        exit_loop_if(x == 10);
        set_variable(x, x + 1);
      }
    }
  ],
  assertions:
    (assert (empty? (Person)) (False)),
    (assert_eq (Eq Person[0] (Person (
      firstName: "Kevin",
      lastName: "Matthews",
      sex: "male",
      age: 50,
      jobid: null
    )))),
    ()
end test;
